@using Sandbox;
@using Sandbox.UI;
@inherits Panel

<root>

    <div class="settings">

        <div class="setting">
            <label class="name">Show Avatars</label>
            <SwitchControl Value:bind="@ChatboxPreferences.Chat.ShowAvatars" />
        </div>

        <div class="setting">
            <label class="name">Font Size</label>
            <SliderControl Value:bind="@ChatboxPreferences.Chat.FontSize" Min=@(10) Max=@(24) Step=@(1) />
        </div>

        <div class="setting">
            <label class="name">Chat SFX</label>
            <SwitchControl Value:bind="@ChatboxPreferences.Chat.ChatSounds" />
        </div>

    </div>

</root>

@code
{

}